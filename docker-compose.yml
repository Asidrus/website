version: '3.8'

services:
  web:
    build:
      context: ./
      dockerfile: Dockerfile
#    command: gunicorn --bind 0.0.0.0:8000 testersite.wsgi
    volumes:
      - static_volume:/app/website/staticfiles
      - media_volume:/app/website/mediafiles
    expose:
      - 8080
    env_file:
      - ./.env
#    depends_on:
#      - db
#  db:
#    image: postgres:13.0-alpine
#    volumes:
#      - postgres_data:/var/lib/postgresql/data/
#    env_file:
#      - ./.env
  nginx:
    build: ./nginx
    volumes:
      - static_volume:/app/website/staticfiles
      - media_volume:/app/website/mediafiles
    ports:
      - 443:80
    depends_on:
      - web

volumes:
  postgres_data:
  static_volume:
  media_volume: